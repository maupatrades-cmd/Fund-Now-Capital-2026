# Agent Coordination Status

## TC-FRAMEWORK LANE — Terms & Conditions Acceptance Framework (Sprint 4, Lane 4d) (2026-08-03)
- Branch: `claude/tc-framework-sprint-4-k1guz4` (fresh main @ `f9b4cf6`, PR #111 in). One PR. **DO NOT merge — owner merges after Macroscope.**
- **Real problem solved (POPIA):** partners (Doctor / Bright Destiny) + contractors had no terms to accept and no auditable record of acceptance. This ships a version-controlled T&C framework: partners + contractors must accept the current terms on first login; every acceptance is immutable legal evidence (who / which version / when + supplementary IP-hash + user-agent).
- **Lane owned (this agent ONLY):** new tables (`terms_config`, `terms_versions`, `terms_acceptances_ledger`) · new RPCs (`accept_terms`, `get_current_terms_for_role`, `check_user_has_accepted_current`) · new components (`src/components/terms/*` incl. `PortalTermsGate`) · new public page (`src/pages/TermsViewPage.tsx`) · new lib+hook (`src/lib/terms.ts`, `src/hooks/useTermsAcceptance.ts`). Shared/partner+contractor files touched ADDITIVELY only: `src/App.tsx` (1 import + 1 public route `/terms/current`); **`src/pages/PartnerGate.tsx` + `src/pages/ContractorGate.tsx`** (wrap their route subtree with `PortalTermsGate` — the acceptance gate, moved here from the home pages in Macroscope R1 so it can't be deep-link-bypassed; **note PartnerGate is DOCTOR-BUILD's file — the change is a single additive wrap of `<Outlet/>`**); `src/pages/PartnerHomePage.tsx` + `src/pages/ContractorHomePage.tsx` (now only a one-line comment noting the gate lives in the route guard — the R1 modal wiring was removed); `src/lib/notifications.ts` (TERMS_UPDATED label+icon); this status.md.
- **Verified against live schema (2026-08-03):** `user_role = {owner,partner,contractor}`; terms tables did NOT exist; pgcrypto (`extensions.digest`) + `request.headers` reachable; `emit_in_app_notification(uuid, notification_event_type, text, text, text, jsonb)`; profiles has `is_active`. Test users: owner `business.lekgoro@gmail.com`, partner `queenasdice@gmail.com`, contractor `maupatrades@gmail.com`.
- **Schema (2 migrations, dry-run verified via non-persisting BEGIN/ROLLBACK against live — DDL applies clean, all RPC bodies compile, structural + behavioural assertions pass (accept/idempotency/immutability/authz), nothing persisted; NOT yet applied):**
  - `20260803160000_terms_framework_enums.sql` — `notification_event_type += TERMS_UPDATED` (own migration so the value commits before part 2's trigger uses it).
  - `20260803160100_terms_framework.sql` — `terms_config` (private single-row random salt; RLS on, no policy, no API grants), `terms_versions` (version-controlled markdown, `applies_to_roles user_role[]`, PUBLIC read of current + owner-all RLS, anon+authenticated SELECT grant), `terms_acceptances_ledger` (append-only, `unique(user_id, terms_version_id)`, immutability trigger blocking UPDATE/DELETE, user-read-own + owner-read RLS, DEFINER-only writes). RPCs all belt-and-braces DEFINER + `search_path=''`: `accept_terms` (advisory lock + **server-side salted SHA-256 IP hash from request.headers**, client `p_ip_hash` = fallback only; idempotent ON CONFLICT DO NOTHING; activity_logs write only on genuine new acceptance), `get_current_terms_for_role` (public info, anon-granted), `check_user_has_accepted_current` (caller-or-owner authz). `terms_versions_notify_updated` trigger fires TERMS_UPDATED to all active applicable users when a version becomes current (created AFTER the silent v1.0 seed). Grants matrix: accept/check = authenticated+service_role+postgres (NOT anon); get_current also anon. Seed: v1.0 current, applies to partner+contractor (owner-editable content later).
- **IP-hash decision (PR #111 pattern, reinterpreted correctly):** PR #111 hashed the IP SERVER-SIDE in its Edge Function. `accept_terms` is browser-called via PostgREST, which exposes the real client IP in `request.headers` — so the RPC hashes SERVER-SIDE (salted, not client-forgeable, no browser privacy leak, raw IP never stored) and PREFERS that over the client value. The brief's "hashes IP client-side then sends" is honored by a best-effort `hashClientIp()` in the modal (fetch public IP + Web Crypto SHA-256, 2.5s timeout, silent null on failure) passed as the fallback. Authoritative evidence = server-set user_id (auth.uid) + accepted_at + version.
- **Frontend:** `TermsAcceptanceModal` (scroll-to-bottom detection enables Accept; Accept has a `useRef` in-flight guard per Fix #4; Decline → `signOutAndRedirect`; dismissable ONLY via Accept/Decline — no X, no backdrop, no Escape; body-scroll locked), wired into both portal home pages as a first-render check keyed by userId (mirrors useProfileRole cache-key). Public `/terms/current` viewer (anon-readable, for the FNC website footer). Dependency-free markdown renderer (`TermsMarkdown` — no react-markdown, mirroring REPORTS-V1's no-recharts call; XSS-safe by construction).
- **Explicitly DEFERRED (per brief, follow-up PR):** owner-side terms admin UI (`/team/terms` — for now owner edits `terms_versions` via SQL/Dashboard; TERMS_UPDATED still fires via the trigger on any is_current→true, so direct edits notify users). English-only. No real signature capture (accept-click IS the POPIA signature). No LoginPage changes (modal renders after login). No `send-notification-email` variant for TERMS_UPDATED — uses the generic fallback (BONUS_PAID/F4 precedent).
- **Did NOT touch (other active lanes):** `usePortalLeads` (LEADS-CACHE-FIX), notification preferences page/matrix logic (NOTIF-PREFS — only added a label/icon to the shared `notifications.ts` map, additive), Doctor invoicing (DOCTOR-INVOICING), deal-detail commission picker (PICKER-UI), any partner/contractor route file beyond the two home-page modal wires.
- **Build:** `tsc -b` ✔ · `vite build` ✔ · `oxlint` ✔ (no new warnings — only pre-existing ConfettiProvider/FilterBar/vapour-text ones).
- **POST-MERGE steps (owner / applier):** 1) apply the 2 migrations to live IN ORDER (`enums` → `terms_framework`); the second runs its behavioural assertions once TERMS_UPDATED is committed. 2) Vercel auto-deploys the frontend on merge. Then the acceptance test (below).
- **Acceptance test (post-merge + apply):** (1) Doctor `queenasdice@gmail.com` incognito → `/partner` → sees T&C modal; scroll to bottom → Accept enables → click → modal closes → home renders. (2) Sign out/in → no modal (accepted current). (3) Same as contractor `maupatrades@gmail.com` at `/contractor`. (4) Public browser → `/terms/current` → sees terms, no login. (5) Owner → NO modal (only partner+contractor must accept). (6) Decline → clean sign-out.
- **Macroscope round 1 (5 findings, all owner-approved + fixed):** (#1/#2 High) relocated the gate from the home pages into `PartnerGate` + `ContractorGate` via a new `PortalTermsGate` that wraps the ENTIRE portal subtree and **fails closed** while loading/errored — an unaccepted user can no longer deep-link past it, and an RPC error blocks rather than opens. (#4 High) `accept_terms` now requires the version to be `is_current` AND the caller's role ∈ `applies_to_roles` before writing the ledger (blocks recording a superseded/other-role acceptance); new negative assertion added + re-dry-run green. (#3 Medium) modal resets `scrolledToBottom` + scrolls to top on `version.id` change. (#5 High) hook invalidates the uid-independent `['terms-acceptance']` prefix + `staleTime:0`. `tsc/vite/oxlint` green; migration re-dry-run against live (nothing persisted).
- Last update: 2026-08-03 (Macroscope R1 fixes applied).

## REPORTS-V1 FOLLOW-UP — fold bonus_records into partner leaderboard (2026-08-03)
- Branch: `claude/jolly-gauss-w799ml` (fresh main @ d61f501, after #108/#109/#110). New PR (REPORTS-V1 #108 already merged). **DO NOT merge — owner merges after Macroscope.**
- **Change:** the partner leaderboard now folds `bonus_records` (C2.4 discretionary owner bonuses) into partner earnings. New columns **Partner Share** (commission `partner_share`) · **Bonus** (Σ `bonus_amount`) · **Total Earned** (sum, default sort). Same windowing rules as commission (money on `earned_at ?? created_at`, void excluded); `dealsFunded` is the union of commission + bonus deal ids; a bonus-earning partner also counts toward the **Active Partners** KPI. **Total Commission Earned KPI is unchanged** — bonuses are partner payouts, not FNC revenue, so they stay out of that figure.
- **Read-only, additive:** `src/lib/reports.ts` (BonusReportRow + normaliseBonus + extended computePartnerPerformance/computeKpis), `src/hooks/useReports.ts` (fifth owner-RLS read query on `bonus_records`), `src/pages/ReportsPage.tsx` (columns + CSV), `.coordination/status.md`. No migration, no schema touch. `bonus_records` schema verified live (0 rows). Build `tsc -b`/`vite build`/`oxlint` green; bonus folding runtime-verified (union deals, void + out-of-range exclusion, total-sort, KPI stays commission-only).
## LEADS-CACHE-FIX LANE — usePortalLeads + usePortalDeals POPIA cache isolation (2026-08-03)
- Branch: `claude/useportalleads-cache-isolation-95i7iy` (fresh main @ f9b4cf6, PR #111 in). One PR. **DO NOT merge — owner merges after Macroscope.**
- **Real problem solved (POPIA cross-user cache leak):** `usePortalLeads` cached its query under the static key `["portal-leads", portal]`, with no session-user dimension. On a same-tab sign-out → sign-in as a *different* user of the same role, TanStack Query would briefly serve the previous user's leads from cache before the refetch resolved. Same bug pattern MY-DEALS-VIEW (PR #107) fixed in `usePortalDeals`, flagged in that PR body as follow-up. RLS was never the leak (server scope is always correct); this is purely the client cache key.
- **Fix (mirrors PR #100 partner-name query · PR #101 `useProfileRole` · PR #107 `usePortalDeals`):** import `useSession`, derive `const uid = useSession()?.user?.id ?? null;`, and add `uid` to the query key → `["portal-leads", portal, uid]`. The mutation's `onSuccess` invalidate still uses the prefix key `["portal-leads", portal]`, which TanStack matches partially — so it continues to clear the per-uid entry after a lead submit (verified: prefix match is the default, `exact` not set).
- **Macroscope round-1 (🔴 Critical, fixed in-PR):** the key alone wasn't enough — `useSession()` is `undefined` while resolving and `null` when signed out, both collapsing to `uid === null`, and the query stayed **enabled** in that window. On first paint the Supabase client can authenticate from its persisted session before React state updates, so an enabled query would fetch real rows and cache them under the placeholder `[…, null]` key — surfaceable by a later signed-out/loading render (the very leak this closes). Added `enabled: uid !== null` to gate the fetch until a concrete uid is known, and returned `isLoading: query.isLoading || uid === null` so the brief resolving window shows the spinner instead of flashing the empty state. Still one file.
- **`usePortalDeals` bundled in (owner-approved, 2026-08-03):** the canonical `usePortalDeals` (PR #107, merged — MY-DEALS-VIEW's file, which explicitly flagged this as this agent's follow-up) had the identical `?? null` + no-`enabled` first-paint shape. Owner approved folding the mirror fix into this same PR rather than a separate one (legitimate lane extension — originally worked with MY-DEALS-VIEW's code). Applied the exact same pattern to `src/hooks/usePortalDeals.ts`: `enabled: uid !== null` + `return { ...query, isLoading: query.isLoading || uid === null }` (guards the `MyDealsList` empty-state flash). This is the 5th surfacing of this bug class (PR #100 partner-name · #101 useProfileRole · #104/#113 usePortalLeads · #107 usePortalDeals cache-key · now the deeper first-paint variant). *(Superseded the briefly-opened standalone PR #117 — closed in favour of this bundle.)*
- **Scope:** TWO files — `src/hooks/usePortalLeads.ts` + `src/hooks/usePortalDeals.ts` (identical fix pattern). No schema, no RPC, no route, no other file except this status section. Consumers (`MyLeadsView`/`MyDealsList`) unchanged.
- **Did NOT touch (other active lanes):** `/partner/*` + `/contractor/*` pages, `useApplications`/`/apply` (APPLY-ROUTE), `/reports` (REPORTS-V1), any migration/RPC (PICKER-BACKEND, TIER-ENGINE), notification prefs (NOTIF-PREFS), Doctor invoicing (DOCTOR-INVOICING). `usePortalDeals` is MY-DEALS-VIEW's merged file — touched here only per their explicit follow-up flag + owner approval.
- **Build:** `tsc -b` ✔ · `vite build` ✔ · `oxlint` ✔ (only pre-existing vapour-text/FilterBar/ConfettiProvider/generate-invoice-pdf warnings — none in the touched file).
- **Acceptance test (post-merge + Vercel deploy):** sign in as partner `queenasdice@gmail.com`, note the rows on `/partner/leads` AND `/partner/deals` → sign out → sign back in as a *different* same-role user in the same tab → neither `/partner/leads` nor `/partner/deals` briefly flashes the previous user's rows (same cache-isolation test as PR #107, now covering both hooks).
- Last update: 2026-08-03 (usePortalDeals fix bundled in; standalone PR #117 closed).

## APPLY-ROUTE LANE — Public Application Entry Point (2026-08-03)
- Branch: `claude/public-apply-route-f2i8o3` (fresh main @ 9d24c28). One PR. **DO NOT merge — owner merges after Macroscope.** Sprint 2, Lane 2a.
- **Real problem solved (ONBOARDING.md Stage 1 + CONTRACTOR.md standing rule):** cold applicants had no way to apply — contractors could only be created by the owner via Team Management. This ships the public `/apply` entry point that creates a `contractors` record at status `applicant`, plus the owner's Applications review tab.
- **Table decision (verified live 2026-08-03):** no `contractors` table existed. CONTRACTOR.md + ONBOARDING.md are explicit that the `/apply` form creates a **`contractors`** record (the ONE non-owner create path) — so this lane creates the canonical `contractors` table, populated with the Stage-1 application subset of columns per this lane's brief (full_name/email/phone/id_number/physical_address/experience_years/motivation_text/referral_source(+_other)/status/rejected_reason/screening_notes). Tax/banking/BEE/service-agreement/progression columns are **additive** follow-ups in later HR phases (G2+). NOT a separate `contractor_applications` table (would contradict canonical truth + force a later data migration).
- **Scope shipped:**
  - **Migration 1 `20260803140000_contractor_application_enums.sql`** — `notification_event_type += CONTRACTOR_APPLICATION_RECEIVED` (own migration so the value commits before use). No new `activity_event_type` value: the create logs as `CREATE` and status transitions as `STAGE_CHANGE` (both already exist).
  - **Migration 2 `20260803140100_contractor_applications.sql`** — `contractor_status` enum (full 9-state lifecycle; Stage-1 uses applicant/screening/rejected only), the `contractors` table (owner-only `contractors_owner_all` RLS, **NO partner/contractor policy** → cross-role isolation; `revoke all` + grant SELECT-only to authenticated → writes only via DEFINER RPCs; `set_updated_at` trigger; experience-bound + rejected-needs-reason CHECKs), and `contractor_application_attempts` (hashed-IP spam ledger, RLS-on + zero policies + no API grants). Structural DO-block assertions.
  - **Migration 3 `20260803140200_contractor_application_rpcs.sql`** — `submit_contractor_application(jsonb, text, int, int)` (**service_role-only** public path: IP throttle → validate → insert `applicant` → PII-minimised `CREATE` activity row → notify every active owner via `emit_in_app_notification`; returns {status:created|rate_limited,...}); `move_application_to_screening(uuid)` + `reject_application(uuid, text)` (owner-only: DEFINER + `search_path=''` + `is_owner()` + advisory lock + state guard + STAGE_CHANGE audit + RETURNING check + idempotent). Grants matrix (submit = service_role/postgres only; owner RPCs = authenticated/postgres/service_role; anon/PUBLIC revoked). Structural + behavioural assertions (behavioural guarded on the enum value existing → runs at real apply, self-skips in a dry-run).
  - **Edge Function `apply-submit-application`** (`verify_jwt=false`, config.toml appended) — the PUBLIC endpoint (no caller auth — anyone can apply). Validates payload, SHA-256-hashes the client IP (POPIA: raw IP never stored), calls the service-role submit RPC, maps `rate_limited`→HTTP 429, sends the applicant a branded Resend confirmation email (mirrors admin-invite-user's shell; contains ONLY the applicant's own name — no data leak). Uses `SUPABASE_SERVICE_ROLE_KEY` + existing `RESEND_API_KEY`/`APP_BASE_URL`; optional `APPLY_IP_SALT`.
  - **Frontend:** public `/apply` route in App.tsx (outside every gate); `PublicApplyPage.tsx` (RHF+zod + FIX#4 useRef in-flight guard, mobile-first, FNC branding, inline success card, optional SA ID field per ONBOARDING); `src/lib/applications.ts` (types + status/referral helpers); `src/hooks/useApplications.ts` (owner list keyed by userId per the portal-cache-key pattern + move/reject RPC mutations + public `useSubmitApplication`); `src/components/team/ApplicationsPanel.tsx` (filter All/Applicant/Screening/Rejected + name/email search + table + ⋮ actions + view drawer + move-confirm + reject-with-reason dialog). `src/lib/notifications.ts` maps the new event (UserPlus icon, prefs matrix picks it up automatically).
  - **TeamPage extension (TEAM-BUILD's file, PR #103 merged):** additive only — imports `ApplicationsPanel`, adds an "Applications" sibling tab (with a live count via the shared `useApplications` cache), deep-links from `/team?tab=applications`, and conditionally renders the panel. The existing invite/edit/deactivate/resend flow is **untouched**.
- **Cross-role isolation (acceptance test #10):** `contractors` + `contractor_application_attempts` have owner-only / no-policy RLS; a partner/contractor SELECT returns zero rows. Applicants have no auth at all — the row is created by the service-role RPC.
- **Verification:** both schema migrations dry-run against live via non-persisting BEGIN/ROLLBACK — DDL applies clean, all 3 functions compile, structural assertions pass, grants matrix correct; a second rolled-back run exercised the FULL runtime flow (submit→owner-notify→audit, validation raise, move+idempotency, reject+double-reject raise, IP rate-limit) via an enum-swapped `submit_test` variant. **Nothing persisted** — confirmed `contractors`/functions/enum absent from live afterwards. `tsc -b && vite build` green; `oxlint` no new warnings.
- **Files NOT touched (other lanes):** `/partner/*` + `/contractor/*` routes (MY-DEALS-VIEW), `/reports` (REPORTS-V1), `commission_records`/`deal_commissions`/`deal_attributions` (TIER-ENGINE), LoginPage/AuthPage, existing App.tsx routes (only ADDED `/apply`), TEAM-BUILD's invite/edit/deactivate flow.
- **POST-MERGE steps (owner / applier):** 1) apply the 3 migrations to live **IN ORDER** (`enums` → `contractors` → `rpcs`; the 3rd runs the behavioural assertions once the enum value is committed); 2) deploy Edge Function `apply-submit-application` with **verify_jwt=false**; 3) Vercel auto-deploys the frontend on merge. Then the acceptance test (visit `/apply` → submit → thank-you + confirmation email + owner notification → Team → Applications tab → View → Move to Screening → Reject-with-reason on a second app → 10-in-1-min → 429).
- Last update: 2026-08-03.

## REPORTS-V1 LANE — owner-only analytics dashboard (Sprint 4 Lane 4c) (2026-08-03)
- Branch: `claude/jolly-gauss-w799ml` (fresh main @ 9d24c28, includes PICKER-BACKEND #106). One PR. **DO NOT merge — owner merges after Macroscope.**
- **Real problem solved:** owner had no analytics surface — commission-by-month, funder revenue, contractor/partner leaderboards all required manual SQL. `/reports` closes that gap.
- **Lane owned (this agent ONLY):** `/reports` route under OwnerGate · "Reports" sidebar entry (between Activity and Team) · `src/pages/ReportsPage.tsx` · `src/components/reports/*` (KpiCard, DateRangeFilter, ReportTable, CommissionByMonthChart) · `src/lib/reports.ts` · `src/hooks/useReports.ts`.
- **READ-ONLY lane — NO migration, NO RPC, NO schema touch.** Client-side aggregation over the live money-truth (`commission_records` + `bonus_records`* — see note), `leads`, `referral_partners`, contractor `profiles`. Decision: the lane brief's optional `get_report_*` helper RPCs are NOT built — owner data volume is tiny and the whole dashboard is ~4 read queries (well under the 20+ bar that would justify a Postgres aggregate). The aggregation fns in `reports.ts` map 1:1 onto DEFINER helper RPCs later if volume grows, with no UI change. *(Partner-share earnings use `commission_records.partner_share`; bonus_records is not yet folded into the leaderboard — a later polish item.)*
- **Verified against live schema (2026-08-03):** `commission_records`/`bonus_records` 0 rows (no genuine funding yet), `deals` 3 (1 at Funded stage, no `amount_funded`), `leads` 3, contractor profiles 2, `referral_partners` 1, `submissions_with_funded` 0. So per the acceptance test, funder/contractor/partner tables render empty states cleanly; KPIs read 0/roster counts. Attribution columns `leads.attributed_to_contractor_id` / `attributed_to_partner_id` confirmed LIVE (LEAD-SUBMIT migration applied). No `contractors`/`badges`/`levels` tables exist → "Current Level" is a documented provisional display bracket over FNC gross (retire when the contractor-milestones module lands).
- **Metrics (all windowed on the header date filter; money windowed on `earned_at ?? created_at`, void excluded; leads on `created_at`):** KPIs = Total Commission Earned (Σ gross_commission) · Deals Funded (distinct) · Active Contractors / Partners (any lead OR funded deal in range). Funder perf = real name · total gross · deals funded · avg deal size (Σ amount_funded / deals w/ amount) · % of total. Contractor perf via deal→lead→`attributed_to_contractor_id`. Partner perf via `referral_partner_id`. Chart = commission by month, FIXED last-12-months (independent of the filter, per brief).
- **CSV export:** client-side, RFC-4180 quoted + formula-injection neutralised, honours current sort order. POPIA-safe (owner data, real names — S7C anonymisation is partner-side only).
- **Charts:** dependency-free SVG/CSS bar chart. recharts is NOT installed; a single 12-bar chart doesn't justify the dep + lockfile churn during a parallel sprint. Matches the dashboard's inline-bar style.
- **Cross-role isolation (POPIA):** `/reports` sits inside the single `<OwnerGate>` block — a signed-in partner is bounced to `/partner`, a contractor to `/contractor` (existing gate behaviour, no new logic).
- **Files touched outside the lane (additive only):** `src/App.tsx` (one import + one route), `src/components/layout/navItems.ts` (one icon import + one nav item), `.coordination/status.md` (this section). No overlap with PICKER-BACKEND (`deal_commissions`/migrations/RPCs) or MY-DEALS-VIEW (`/partner/*`, `/contractor/*`, `usePortalDeals`).
- **Build:** `tsc -b` ✔ · `vite build` ✔ · `oxlint` ✔ (no new warnings). Aggregation logic runtime-verified against a synthetic dataset (KPIs, funder %, avg deal size, void/out-of-range exclusion, attribution, CSV escaping, presets).
- **Acceptance test (post-merge + Vercel deploy):** owner → "Reports" in sidebar → default "Last 30 days" → 4 KPI cards + 3 tables (empty states) + chart; change range refreshes; Export CSV downloads; partner hitting `/reports` → `/partner`, contractor → `/contractor`.
- Last update: 2026-08-03.
## MY-DEALS-VIEW-SHELL LANE — partner + contractor "My Deals" read-only view (2026-08-03)
- **✅ PR #107 MERGED + MIGRATION APPLIED + VERIFIED LIVE (2026-08-03).** `20260803090000_my_deals_view_rpcs.sql` applied via `apply_migration`; DO-block assertions passed. Verified live: `partner_list_own_deals` + `contractor_list_own_deals` both `SECURITY DEFINER`, `search_path=''`, EXECUTE granted to `authenticated,postgres,service_role` only (no anon/public). Macroscope re-review clean (cache-key finding fixed in `0a23a02` + thread resolved). Vercel deployed frontend on merge. Both "My Deals" tabs now resolve (empty state today — 0 attributed leads qualified into deals yet).
- Branch: `claude/my-deals-view-shell-e3w2z0` (fresh main @ `a1b2ae5`, PRs #104/#105 in). One PR. **PR #107 MERGED (owner merged after Macroscope re-review clean).**
- **Real problem solved:** partners (Doctor / Bright Destiny) + contractors need to see the deals that grew out of the leads THEY referred — with a partner-safe status and an anonymised funder name. Read-only shell; **no earnings/commission display** (that's a follow-up PR after PICKER-BACKEND merges).
- **Schema reconciliation (verified against live 2026-08-03 — the brief's assumed shape did NOT match live):** `deals` carries **no** submitter-attribution column (only `referral_partner_id` → referral_partners.id; contractors have no referral_partner at all) and **no** `submitted_at`. Submitter attribution lives on `leads.attributed_to_partner_id` / `attributed_to_contractor_id` (= auth.uid(), from LEAD-SUBMIT PR #104); a deal links back via `deals.lead_id` (set by qualify_lead). **So the only POPIA-correct, role-uniform submitter→deal path is `deals.lead_id → leads.attributed_to_{partner,contractor}_id = auth.uid()`** (INNER JOIN leads → legacy DEAL-001 with no lead is correctly invisible). `submitted_at` = the originating lead's `created_at`.
- **Lane owned (this agent ONLY):**
  - **Migration `20260803090000_my_deals_view_rpcs.sql`** — two read-only SECURITY DEFINER RPCs `partner_list_own_deals()` / `contractor_list_own_deals()`. Belt-and-braces: `search_path=''`, role re-check (partner/contractor respectively) + `is_active` guard from profiles, strict `auth.uid()` filter; returns deal_id, deal_reference, client_business_name, current_stage (raw 15-value `deal_stage` as text), anonymized_funder_name (via existing `funder_display_name(awarded_funder_id, uid)` → fictional name only), submitted_at (lead created_at). **No commission/earnings columns.** Grants authenticated + service_role; anon/public revoked (FIX-A posture). DO-block assertions (exists / SECURITY DEFINER / grants matrix). **Dry-run verified via BEGIN/ROLLBACK, then applied to live + verified 2026-08-03 (assertions passed; both RPCs SECURITY DEFINER, grants matrix correct).**
  - `src/hooks/usePortalDeals.ts` — `usePortalDeals(portal)` (calls the role-appropriate RPC, cache keyed by portal) + `dealStatus()` (15 owner stages → 6 partner-facing labels New/Qualified/Submitted/Approved/Funded/Declined) + `DEAL_STATUS_BADGE`.
  - `src/components/portal/MyDealsList.tsx` — shared table used by both portals (Business / Funder / Stage badge / Submitted relative-time). Loading + error(retry) + empty state (`EmptyState` primitive; CTA → `/{portal}/submit-lead`).
  - `src/pages/partner/PartnerDealsPage.tsx` + `src/pages/contractor/ContractorDealsPage.tsx` (PortalShell + MyDealsList, mirroring the MyLeadsPage wrappers).
  - Routes: `/partner/deals` (App.tsx, inside PartnerGate block) + `/contractor/deals` (ContractorGate's own `<Routes>`, inside ContractorGate).
  - "My Deals" nav entry added to the shared `PortalShell` navItems (both portals get it — the brief's "sidebar entry in Partner + Contractor sidebar").
- **POPIA cross-role isolation:** partner RPC filters `attributed_to_partner_id = auth.uid()`, contractor RPC `attributed_to_contractor_id = auth.uid()`; the two columns are role-exclusive + each RPC is role-gated, so a partner can never see a contractor's deals and vice-versa. Funder identity never leaves the DB un-anonymised. Owner unaffected (owner has no partner/contractor role → RPCs raise; owner uses the CRM directly).
- **Shared files edited (additive only, expected merge points):** `src/App.tsx` (1 import + 1 route), `src/pages/ContractorGate.tsx` (1 import + 1 route), `src/components/portal/PortalShell.tsx` (1 nav item + icon import), `.coordination/status.md` (this section). PortalShell is LEAD-SUBMIT's file but merged to main; my lane owns adding the "My Deals" entry per brief.
- **Did NOT touch (other active lanes):** `deals` table / `activity_event_type` enum / any `calc_*`/`set_commission_picker`/`transition_to_*`/commission-picker fn (PICKER-BACKEND PR #106); `contractors` table + `/apply` route (CONTRACTOR-APPLICATION); `admin_*` RPCs (Team); sign-out buttons; `MyLeadsView`/`usePortalLeads`/`partner_submit_lead`/attribution migration (LEAD-SUBMIT — read/reused only). Migration timestamp `20260803090000` is 2026-08-03, distinct from every existing + LEAD-SUBMIT (2026-08-02) file.
- **Follow-up (next PR, after PICKER-BACKEND + TIER-ENGINE merge):** add the earnings column (Doctor's Rands, 50/50 framing per SPEC S7C — never gross/tier/pool) to `MyDealsList` + extend the RPCs.
- **Build:** `tsc -b` + `vite build` green; `oxlint` no new warnings (only pre-existing vapour-text/FilterBar/ConfettiProvider warnings remain).
- **POST-MERGE steps (owner/applier):** 1) ~~apply `20260803090000_my_deals_view_rpcs.sql` to live~~ **DONE (applied + verified 2026-08-03)**; 2) ~~Vercel auto-deploys frontend~~ **DONE (deployed on merge)**. Then acceptance test (below).
- **Acceptance test (post-merge + apply):** Doctor `queenasdice@gmail.com` → `/partner` → "My Deals" in nav → sees deals from leads he referred (anonymised funder + status), or the empty state if none of his leads are qualified into deals yet. Contractor `maupatrades@gmail.com` → `/contractor` → "My Deals" → same, own deals only. Partner cannot see contractor's deals; contractor cannot see partner's; owner unaffected. *(Note: with 0 attributed leads live today, both correctly show the empty state until the owner qualifies an attributed lead into a deal.)*
- Last update: 2026-08-03 (PR #107 merged + migration applied + verified live).

## PICKER-BACKEND LANE — Commission Picker backend (data model + engine + RPCs) (2026-08-03)
- Branch: `claude/commission-picker-backend-model-msjrrw` (fresh main @ c99dc06). One PR. **DO NOT merge — owner merges after Macroscope.** Sprint 1, Lane 1a. **No UI** (the picker UI is a follow-up PR by a different agent).
- **Real problem:** funders pay FNC differently per deal (PICKER.md). At fund-time the owner picks 4 dimensions (A base / B tier modifier / C timing / D adjustments) and the commission moves POTENTIAL → PENDING → LOCKED. This PR is the BACKEND that stores + calculates per-picker selection.
- **Scope shipped (2 migrations after Macroscope round-1 restructure, verified via non-persisting BEGIN/ROLLBACK dry-run against live — DDL applies clean, all 14 function bodies compile, calc math + validation-hardening + grants matrix pass, nothing persisted; NOT yet applied):**
  - `20260803120000_picker_backend_activity_enums.sql` — `activity_event_type` += `COMMISSION_PICKER_SET`, `COMMISSION_PICKER_UPDATED`, `COMMISSION_STATE_TRANSITION` (own migration so the values commit before use).
  - `20260803120100_picker_backend.sql` — (1) **owner-only `deal_commissions` table** (1:1 with deals, `deal_id` PK/FK; `commission_calculation jsonb`, `commission_state text` CHECK potential/pending/locked, `estimated_gross_commission`/`actual_gross_commission numeric(14,2)`, `commission_locked_at`, `commission_locked_by uuid`→profiles; `deal_commissions_locked_shape` CHECK enforcing actual+stamps iff locked; `deal_commissions_owner_all` RLS, **NO partner policy**; **INSERT/UPDATE/DELETE/TRUNCATE revoked from authenticated** so only DEFINER RPCs write; empty table → inline FKs/indexes, no CONCURRENTLY dance). (2) 7 calc-base SECURITY DEFINER helpers (rate = whole-number percent, all `numeric(14,2)`, authenticated-only). (3) 5 state RPCs `set_commission_picker` / `update_commission_picker` / `transition_to_pending` / `transition_to_locked` / `unlock_commission` — DEFINER + `search_path=''` + `is_owner()` + advisory lock + state guard + `activity_logs` write + idempotent no-op; grants authenticated+postgres+service_role, anon/PUBLIC revoked. (4) 2 read RPCs `get_deal_commission_state` / `list_deals_by_commission_state` (owner-only). (5) internal helpers `log_commission_picker_activity` + `validate_commission_calculation` (no API grant). Structural + behavioural DO-block assertions (behavioural runs at real apply — inserts the new enum values).
- **Macroscope round-1 findings (all 4 resolved in R2, owner-approved):** 🔴 Critical (partner column exposure via `deals_partner_read_own`) + 🟠 High (owner direct-write bypassing the state machine) → **fixed together** by moving the 6 picker fields off `deals` into the owner-only `deal_commissions` table with no partner policy and write-revoked from `authenticated` (RPC-only via DEFINER); dropped the round-1 CONCURRENTLY follow-up migration. 🟡 Medium (validation) → `validate_commission_calculation` now checks `rate` (present + numeric for the 5 percent bases; optional-but-numeric otherwise) and string-only array elements. 🟡 Medium (audit) → `set`/`update` capture prior calc/est/state before the write so `before_values` is truthful (was before==after). A dry-run also caught a self-found gap: the round-1 `revoke insert,update,delete` left `authenticated` with TRUNCATE (bypasses RLS) — now `revoke all` + grant-back SELECT only.
- **Explicitly OUT of scope (later PRs, per brief):** UI; partner "Potential Earning" display; C2 money-engine hand-off (`write_commission_record`) at LOCK; Sprint-1b tier engine; notification triggers on state transitions.
- **Privacy (POPIA + commission privacy):** all commission data lives in the owner-only `deal_commissions` table (owner-ALL RLS, no partner policy, writes revoked from authenticated); every RPC also gates on `is_owner()`. Partners can never reach FNC gross.
- **Files touched:** the 2 migrations above + this status.md section only. No frontend, no other lane's files. Zero collision with ROADMAP-UPDATE (PR #105, ROADMAP.md only) or any other active lane.
- **⚠️ Live-DB note (my error, disclosed):** during verification an accidental probe permanently added an inert, unused label `ZZ_PROBE_TMP` to the live `activity_event_type` enum. It has no emitter and no rows; Postgres has no `DROP VALUE` and the project role can't DELETE from `pg_enum`, so in-place removal would require recreating the enum type (heavy/risky on prod — not done). Recommend leaving it as a benign artifact (like AUDIT.md §1.4 cosmetics) unless the owner wants a dedicated type-recreation migration later. The picker migration itself is unaffected.
- **APPLIED TO LIVE (2026-08-03, post-merge):** both migrations applied in order + verified — `deal_commissions` table live (owner-only RLS, single `deal_commissions_owner_all` policy, no partner policy), `authenticated` grants = `{SELECT}` only (INSERT/UPDATE/DELETE/TRUNCATE revoked, anon none), all 14 functions present, 3 new `activity_event_type` values live, 0 rows. Ledger: `picker_backend_activity_enums` + `picker_backend` (apply-time version, short name — matches the `contractor_role`/`fix_a_rpc_grants_hardening` precedent).
- **APPLY-TIME HOTFIX (this follow-up PR):** the first live apply of the merged migration FAILED — `list_deals_by_commission_state` used `row_to_jsonb(t)`, which does not exist in PostgreSQL (correct fn is `to_jsonb`). The migration's own behavioural assertions caught it and rolled the whole apply back cleanly (nothing partial). Slipped past the pre-merge dry-run because that run created the function but never *executed* it (behavioural section excluded — can't use the new enum values in the same tx they're added). Fix: one line, `row_to_jsonb` → `to_jsonb`. Live was applied with the corrected version; this PR brings the repo file to parity. Every other state-machine assertion executed on live and passed before the failing line.
- **POST-MERGE steps for THIS follow-up PR (owner / applier):** live is ALREADY correct (applied with the fix) — no re-apply needed; merging this PR only reconciles the repo file so a fresh `supabase db reset`/re-apply reproduces cleanly.
- Last update: 2026-08-03 (R2 applied to live + `to_jsonb` apply-time hotfix follow-up PR).

## LEAD-SUBMIT LANE — partner + contractor lead submission forms (2026-08-02)
- Branch: `claude/lead-submit-forms-bo10x4` (fresh main @ c2cb393). One PR. DO NOT merge — owner merges after Macroscope.
- **Depends on (already merged + live):** CONTRACTOR-BUILD (`contractor` role enum live), DOCTOR-BUILD (`PartnerGate`/`PartnerHomePage`). Verified live: `user_role = {owner,partner,contractor}`; test users owner `business.lekgoro@gmail.com` / partner `queenasdice@gmail.com` (→ Bright Destiny `9f48c460…`) / contractor `maupatrades@gmail.com`.
- **Lane owned (this agent ONLY):** `/partner/submit-lead` + `/partner/leads` + `/contractor/submit-lead` + `/contractor/leads` routes & pages · `src/components/portal/*` (PortalShell nav, LeadSubmitForm, MyLeadsView) · `src/hooks/usePortalLeads.ts` · `partner_submit_lead` + `contractor_submit_lead` RPCs · leads/documents/storage RLS additions (partner + contractor scoping) · "Submit a new lead" CTA + nav on both home pages.
- **Schema (3 migrations, verified via non-persisting BEGIN/ROLLBACK dry-run against live — DDL applies clean, both RPC bodies compile, grants matrix correct; NOT yet applied):**
  - `20260802120000_lead_submit_enums.sql` — `document_upload_source` += `contractor`; `notification_event_type` += `LEAD_SUBMITTED_BY_CONTRACTOR` (own migration so the values are committed before use).
  - `20260802120100_lead_submit_attribution.sql` — `leads.attributed_to_partner_id` / `attributed_to_contractor_id` (uuid → auth.users, FK NOT VALID + VALIDATE, partial indexes); `caller_owns_lead(uuid)` DEFINER helper (authenticated-only grant); leads SELECT policies `leads_partner_read_own` / `leads_contractor_read_own` (`attributed_to_*_id = auth.uid()`); documents `documents_lead_submitter_read` (PII-exclusion backstop) + `documents_lead_submitter_insert`; storage `documents_bucket_submitter_insert` (`leads/{lead_id}/…` path, own-lead check). DO-block assertions.
  - `20260802120200_lead_submit_rpcs.sql` — `partner_submit_lead` / `contractor_submit_lead` (belt-and-braces DEFINER: role re-check from profiles, required-field guards, sets attribution + `entered_by`, notifies every active owner via `emit_in_app_notification`; the existing leads triggers give activity_logs + CIPC-block for free). authenticated-only grants; anon/public revoked. DO-block assertions.
- **POPIA cross-role isolation:** partner sees ONLY `attributed_to_partner_id = auth.uid()` rows, contractor ONLY `attributed_to_contractor_id`; the columns are role-exclusive so neither can ever see the other's leads. Owner keeps full access (`leads_owner_all` / `documents_owner_all` untouched).
- **Notifications:** reuses existing `LEAD_SUBMITTED_BY_PARTNER` for partner; adds `LEAD_SUBMITTED_BY_CONTRACTOR` for contractor. `src/lib/notifications.ts` maps both (label + Handshake icon). NOT a badge event — clear of the BADGES lane. Email uses the send-notification-email generic fallback (a dedicated variant is optional F4/S16 follow-up, not built here).
- **⚠️ SHARED FILE with BADGES lane:** `src/pages/ContractorHomePage.tsx` — this lane adds the "Submit a new lead" CTA + nav (rewrites the shell onto PortalShell, keeps a content-stack card BADGES can extend). BADGES adds its badge CARD to the same content stack. Both branch from the same main; owner resolves the one expected merge overlap (additions to the content stack). No other file overlaps any active lane.
- **Files NOT touched (other lanes):** `/team/*`, `admin_*` RPCs, `badges`/`contractor_milestones` tables + badge triggers, the badge card component, badge-earning notification enum values.
- **POST-MERGE steps (owner):** 1) apply the 3 migrations to live IN ORDER (`enums` → `attribution` → `rpcs`); 2) Vercel auto-deploys the frontend on merge. Then run the acceptance test (below).
- **Acceptance test (post-merge + apply):** partner `queenasdice@gmail.com` → `/partner` → "Submit a new lead" → fill + attach a doc → submit → "Lead submitted for review" toast → `/partner/leads` shows it (status New). Owner inbox `/leads` shows it (referred_by Bright Destiny) + bell notification "New lead submitted by Bright Destiny". Same as contractor `maupatrades@gmail.com` (notification names the contractor). Partner cannot see contractor's lead; contractor cannot see partner's; owner sees both.
- `tsc -b && vite build` + oxlint green (no new warnings). Last update: 2026-08-02.

## TEAM-BUILD LANE — Team Management module (2026-08-02)
- Branch: `claude/team-management-module-8kutix` (fresh main — contractor + partner foundations already merged). One PR. **DO NOT merge — owner merges after Macroscope.**
- **Real problem solved:** adding a partner/contractor previously required manual SQL in the Supabase dashboard. This ships the owner-facing CRM screen to invite, list, edit-role, and deactivate people.
- **Scope shipped:**
  - **Migration `20260802100000_admin_update_user_role.sql`** — `admin_update_user_role(p_user_id, p_new_role user_role, p_new_referral_partner_id default null)`, SECURITY DEFINER + `search_path=''` + `is_owner()` gate. Refuses to assign/modify the owner role; **clears `referral_partner_id` for any non-partner role** (mirrors the contractor-role migration's `current_partner_id` role guard — the write-time complement); RETURNING+FOUND check (silent-RLS rule); writes a `PERMISSION_CHANGE` `activity_logs` row; grant matrix authenticated-only (FIX-A rule) + DO-block self-checks. **Apply to live AFTER merge.**
  - **Edge Function `admin-invite-user`** (verify_jwt=false; verifies caller Bearer JWT + `is_owner()`, then service-role for admin work). Creates the Auth user (`auth.admin.createUser`, `email_confirm=true`), upserts profile (adds `phone_number`, which `handle_new_user` doesn't set), delivers access via **magic link (Resend)** or **temp password** (returned to owner). **Idempotent** (same email+role → returns existing; different role → 409). Writes audit rows. Also backs the row-level "Resend invite".
  - **Edge Function `admin-deactivate-user`** (same auth model). Soft-delete: **bans** the Auth user (`ban_duration` — blocks login + kills sessions on refresh) + `profiles.is_active=false` + audit. Guards: can't deactivate the owner or yourself; idempotent if already deactivated.
  - `config.toml`: both functions `verify_jwt = false` (documented — CORS preflight + clean JSON errors; is_owner() is the real gate).
  - Frontend: `/team` route under OwnerGate, sidebar "Team" (between Activity and Industries), `src/pages/TeamPage.tsx`, `src/hooks/useTeam.ts`, `src/lib/team.ts`. Filter tabs (All/Owners/Partners/Contractors/Deactivated), table (Name/Email/Phone/Role/Status/Joined/⋮), Invite dialog (magic-link default / temp-password), temp-password copy-to-clipboard modal, Edit Role dialog (+ re-auth/referral-reset warning), Deactivate dialog (type `DEACTIVATE`).
- **Schema verified live (2026-08-02):** `user_role = owner|partner|contractor`; `profiles.role` NN default partner, `profiles.is_active` bool (no `status` column — deactivation uses `is_active`); `handle_new_user` sets full_name/role/referral_partner_id from `raw_user_meta_data` but NOT phone; `is_owner()` reads `profiles.role`; `activity_logs` written by DEFINER/service-role (no INSERT policy). Edge-function auth pattern (AUDIT §4): existing functions use verify_jwt=false + webhook secret for **server-to-server**; these are **browser-called**, so they verify the caller's JWT via `is_owner()` instead — documented in each function header.
- **NOT touched (respecting parallel lanes):** the 3 sign-out buttons (Owner TopBar, ContractorHomePage, PartnerHomePage — CONTRACTOR-BUILD lane); `src/pages/PartnerGate.tsx`/`PartnerHomePage.tsx`, `src/pages/Contractor*`, `src/lib/roles.ts` (imported read-only), `src/lib/notifications.ts`. Only shared files edited: `App.tsx` (one route), `navItems.ts` (one item), `config.toml` (append), `.coordination/status.md` (this section).
- **Build:** `tsc -b && vite build` green; `oxlint` no new warnings. No secrets added — `RESEND_API_KEY` / `APP_BASE_URL` are project-wide and already set.
- **Post-merge steps (owner / applier):** 1) apply migration `20260802100000_admin_update_user_role.sql` to live; 2) deploy Edge Functions `admin-invite-user` + `admin-deactivate-user` with **verify_jwt=false**; 3) acceptance test per the brief (owner sees Team → invites via magic link to a spare email → recipient logs in → temp-password path → edit role → deactivate).
- Last update: 2026-08-02.

## CONTRACTOR-BUILD LANE — contractor role + portal foundation (2026-08-02)
- Branch: `claude/contractor-role-portal-routing-urj72p` (fresh main @ d62c581). One PR. DO NOT merge — owner merges after Macroscope.
- **Scope shipped in this PR:**
  - Migration `20260802090000_contractor_role.sql` — adds `contractor` to the live `public.user_role` enum (verified live 2026-08-02: role lives on `profiles.role`, bootstrapped by `handle_new_user` from `raw_user_meta_data->>'role'`; NOT a JWT claim, NOT a separate table). No new tables/RPCs → no grant-matrix change; DO-block assertions (exact 3 labels; zero RLS policies referencing contractor). **Apply to live AFTER merge.** Contractor gets zero data access by default: partner surfaces scope via `current_partner_id()` (NULL for contractors), everything else `is_owner()`.
  - `src/lib/roles.ts` — `roleHome()` single source of truth: owner → `/dashboard`, partner → `/partner`, contractor → `/contractor`.
  - Three-way post-login redirect (AuthPage) + role-aware `/` landing (App.tsx `RoleLanding`).
  - App.tsx routes: `/partner/*` → `PartnerGate` (DOCTOR-BUILD's file) · `/contractor/*` → `ContractorGate` (mine).
  - `src/pages/ContractorGate.tsx` (session+role guard, owns its subtree) + `src/pages/ContractorHomePage.tsx` (minimal shell: header/logo/logout, welcome, two placeholder cards).
  - OwnerGate: partner/contractor hitting any owner route now bounce to their own portal (was: static "Owner access only" card); unknown-role card retained.
- **⚠️ MERGE ORDER (owner):** DOCTOR-BUILD's PR (creates `src/pages/PartnerGate.tsx`) must merge **before or together with** this PR — App.tsx imports it, so main won't build (Vercel deploy fails) if this merges alone. Verified: with PartnerGate present, tsc+vite build+oxlint all green; without it, the missing import is the only error.
- **Contract for DOCTOR-BUILD:** App.tsx mounts `<Route path="/partner/*" element={<PartnerGate />} />` — PartnerGate must default-export a component that owns the whole `/partner` subtree (session + partner-role check inside; render PartnerHomePage itself or via descendant `<Routes>` with relative paths). Mirror ContractorGate.tsx if useful. Note: the live role enum value is **`'partner'`** (not `referral_partner`); `roleHome()` in `src/lib/roles.ts` is the shared redirect map — use it, don't hand-roll.
- Files NOT touched (DOCTOR-BUILD lane): `src/pages/PartnerHomePage.tsx`, `src/pages/PartnerGate.tsx`, `src/pages/partner/*`, `src/components/partner/*`, `src/lib/notifications.ts`.
- **Macroscope findings (both owner-approved + applied in-PR):** (1) Critical — role-conversion SQL now also clears `referral_partner_id`, AND the migration hardens `current_partner_id()` with `and p.role = 'partner'` so partner-scoped RLS/views can never authorize a non-partner regardless of column state (zero behaviour change for the 3 live profiles); (2) Medium — `useProfileRole` cache now keyed by user id + `ContractorGate` watches the live session (same pattern as PR #100).
- **Post-merge steps:** 1) apply migration to live; 2) owner assigns the contractor role to the existing test user `maupatrades@gmail.com` (auth user + profiles row already live, currently role `partner` via the handle_new_user default) in the Dashboard SQL editor — `update public.profiles set role = 'contractor', referral_partner_id = null where id = (select id from auth.users where email = 'maupatrades@gmail.com') returning id, email, role, referral_partner_id;` — expect exactly 1 row back (profile id `ce628fd1-5280-450a-8c14-71b83e2d4af0`, `referral_partner_id` null); sign that account out/in before testing; 3) acceptance: owner login → /dashboard · Doctor → /partner · contractor (maupatrades@gmail.com) → /contractor welcome shell · contractor hits /dashboard → bounced to /contractor · owner hits /contractor → bounced to /dashboard.
- **Known follow-up (owner, 2026-08-02 — small hygiene PR after both foundation PRs merge):** static-query-key sweep per DOCTOR-BUILD's PR #100 heads-up — this PR already re-keyed `useProfileRole` by user id; audit the remaining pre-existing hooks for the same static-key pattern (lower stakes — role-level data, not names) and re-key any that cache per-user data.
- Out of scope (later PRs): lead submission form, `/apply` application form, onboarding wizard, training, commission display, `contractors` table.
- **MERGE-ORDER RESOLVED (2026-08-02):** DOCTOR-BUILD's PR #100 merged to main first (correct order); this branch merged origin/main in — `PartnerGate.tsx` + `PartnerHomePage.tsx` now present, App.tsx `/partner/*` route resolves, full build green. Only conflict was this file (both lanes prepended a section — both kept).
- Last update: 2026-08-02.

## DOCTOR-BUILD — Partner portal welcome screen (2026-08-02)
- Branch: `claude/partner-portal-welcome-fbd8kz` (fresh main, d62c581). One PR; Macroscope reviews; **owner merges — agent does NOT merge**.
- Lane (this agent ONLY): `src/pages/PartnerGate.tsx` + `src/pages/PartnerHomePage.tsx`. Does NOT touch LoginPage/AuthPage, App.tsx, migrations, or anything in the CONTRACTOR-BUILD lane (`contractor` role migration, post-login redirect, App.tsx `/partner/*` + `/contractor/*` route entries, `src/pages/Contractor*`, `src/pages/contractor/*`, `src/components/contractor/*`).
- **Schema verified live before coding:** the referral-partner role is stored as `profiles.role = 'partner'` (`user_role` enum = `owner | partner` — there is NO `referral_partner` enum value). Partner identity: `profiles.referral_partner_id` → `referral_partners` (columns: id, name, contact_email, contact_phone, is_active, notes, slug — **no logo/branding columns**, so the header uses FNC branding; partner logos are C4/S11.2 territory). RLS confirmed: `profiles_select_own` + `referral_partners_partner_read_own` let a partner read exactly their own two rows.
- **Note for CONTRACTOR-BUILD:** `PartnerGate` checks `role === 'partner'` and works as a layout route (`<Outlet />`) or a children wrapper — mount `/partner/*` however suits App.tsx. Non-partners are redirected to `/login` per the brief (today `/login` falls through the `*` catch-all to `/` = AuthPage; harmless either way).
- Built: PartnerGate (role guard) + PartnerHomePage (FNC-branded header + sign-out, welcome using `referral_partners.name` — test partner renders "Bright Destiny", EmptyState "referred deals" placeholder, disabled "Submit a new lead" placeholder). No schema changes. `tsc && vite build` + oxlint clean (no new warnings).
- Acceptance test after BOTH PRs merge + Vercel deploy: queenasdice@gmail.com logs in → contractor-lane redirect → `/partner` → gate passes → welcome screen with partner name.
- Status: PR #100 MERGED to main (2026-08-02).

## AUDIT-FIX LANE — full system audit + sequenced fixes (2026-07-31)
- **Audit ✅ COMPLETE — `AUDIT.md`** (on branch `claude/fnc-crm-audit-fix-n48rvm`, awaiting owner merge). Read-only inventory of live `hvxruwkgmhjoypepffgv`: 29 tables/RLS/triggers/row counts, DEFINER RPC matrix (locks/idempotency/state guards/grants), Edge Functions, pg_cron (all green), routes, action buttons, notification events, commission-logic sites, advisors. **No blockers.** It supersedes the never-committed `.coordination/full-system-audit-2026-08-01.md` this file previously cited.
- **Owner-approved fix order: FIX-C (docs) → FIX-A (grants migration) → FIX-B (notification prefs matrix).** One PR each, fresh main, Macroscope, owner merges.
- **FIX-C — docs reconciliation — ✅ MERGED (PR #95, 2026-07-31).** Both Macroscope findings addressed in-PR (BONUS_PAID generic-fallback truth; S4 enum list completed to 30 values).
- **FIX-A — RPC grants hardening — ✅ MERGED (PR #96) + APPLIED + VERIFIED LIVE (2026-08-02).** Advisor after apply: anon DEFINER WARNs 14→0, mutable search_path 1→0, trigger-fn authenticated WARNs all cleared; remaining WARNs are the intentional authenticated RPC surface + F5 views (Phase D) + extensions (Phase F) + leaked-password (owner toggle). Verified live: authenticated intact on the API 10, zero API-role grants on the 29 internal fns, `leads_qualification_guard` pinned, `supabase_auth_admin` grant on `handle_new_user`. Residual cosmetic: 6 pure INVOKER helpers (`current_sast_date`, 4× `document_type_*`, `fnc_gross_commission`) still anon-granted — stateless, unflagged, out of approved scope.
- **FIX-B — 🔨 THIS PR** (`claude/fix-b-bonus-paid-email-variant`). Scope shrank: **PR #94 (merged) already delivered all 30 labels/icons/colours AND the prefs matrix** (which iterates `NOTIFICATION_EVENT_TYPES`); the prefs page displays missing rows as ON (`?? true`) matching the Edge Function default, and toggles upsert — so the F4 frontend control gap is closed. The one remaining F4 item: `send-notification-email` had no `BONUS_PAID` variant (generic fallback — Macroscope #95). This PR adds the **`bonus_paid`** variant (commission_paid layout + bonus copy, the `deal_funded` reuse pattern), `eventCategory` → `commission`, SPEC S4 + docs/email-templates.md updated. 12-check render test PASS. **After merge: redeploy `send-notification-email` (v12 → v13).**
- After all 3: separate scope proposal for the two S7C compliance items (flat-40% PO scope, R1M+ tier). **No commission-math change without explicit owner sign-off.**
- Owner-side dashboard tasks (no PR): leaked-password protection toggle; `short_code` for Business Partners + Better Banc; delete inert `pdflibtest` + `ops-storage-remove` Edge Functions. Architectural flag (Phase D planning): the 4 `partner_*` SECURITY DEFINER views.
- Last update: 2026-07-31 — FIX-C in flight.

## FIX LANE — audit follow-up (2026-07-31) — historical, folded into AUDIT-FIX lane above
Sequenced fixes, ONE PR at a time; owner merges each before the next. Do NOT merge — owner merges. Wait for Macroscope.
- **FIX #1 — Docs drift — ✅ MERGED (PR #92, 2026-07-31).** Docs-only. Reconciled funder count `21→23` (CLAUDE.md build-state, ROADMAP A1 annotation, SPEC S1 C0.3 `43+/21→43+/23`) and Polokwane address `75→73` (SPEC S16.3 + S7B.4 footer notes reflect the deployed v12 fix, docs/email-templates.md locked footer block). No code/DB changes.
- **FIX #2 — Lula + RM Capital rate cleanup — ✅ MERGED (PR #93) + APPLIED + VERIFIED LIVE (2026-07-31 audit spot-check: `funder_commission_structures` = exactly 4 rows, all `is_contracted=true` — Business Partners / Flow48 / Merchant Capital / Pollen).**
- **FIX #3 — notification event labels/icons — PR #94 OPEN** (`claude/fix-3-notification-icons`, awaiting Macroscope + owner merge).
- FIX #4–#5 — not started (sequencing now governed by the AUDIT-FIX lane above).

## OLD CC 1 — C2 Backend — ✅ CLOSED OUT (handover items all resolved as of 2026-07-31)
- Session: 2026-07-22 → 2026-07-27. C2.1/C2.2/C2.3 MERGED + APPLIED + verified live.

### ✅ RESOLVED — C2.4 applied (was "next session's first job")
- **PR #86 (C2.4) — MERGED + BOTH MIGRATIONS APPLIED IN ORDER + VERIFIED LIVE** (audit spot-check 2026-07-31): `bonus_records` table live with lifecycle triggers, `BONUS_PAID` present in both `activity_event_type` and `notification_event_type`, 0 rows, no leaked test deals.

### ✅ PR #87 (docs) — MERGED
- CLAUDE.md DEAL-001 correction (Funded stage but NOT genuinely funded; commission_records legitimately 0 rows; never backfill from notes).

### 6 triage findings — status (2026-07-31)
1. Test residue (DEAL-011/012 "reginald maupa") — **DONE** (owner cleaned).
2. Stale docs PRs #64/#39/#29 — **STILL OPEN**, recommend close/renumber (#64's "S7B" label clashes with merged #81's S7B).
3. BONUS_PAID (+ missing event) frontend mapping in src/lib/notifications.ts — **PR #94 OPEN**; full 30-event coverage lands as audit FIX-B after #94.
4. Email footer 75→73 Marshall St — **DONE via PR #84** (merged; v12 deployed + verified).
5. G7 commission-engine reconciliation — **OPEN, promoted to Open Decision 3** (CLAUDE.md/ROADMAP): (a) flat-40% for ALL PO deals vs S7C Sourcefin-only; (b) R1M+ tier 25% in-code vs "TBD" in S7C. Separate scope proposal after audit FIX-A/B/C; no engine change without owner sign-off.
6. DEAL-001 at kanban "Funded" but not genuinely funded — **OWNER JUDGMENT, still open** (move back vs progress properly). Money layer already ignores it correctly.

### C3 — 🔨 PARTIAL (updated 2026-07-31)
- **Owner-side `/partner-earnings` reconciliation view MERGED (PR #91).** Remaining C3: the S8 `doctor_earnings` lifecycle (earned → ready_to_invoice → invoiced → paid) + statements plumbing; P0002 settle/unsettle stubs wire up when C4 builds partner_invoices. Respect SPEC S7C presentation policy (Doctor sees only his Rands, 50/50 framing) + S8. Same rhythm: schema-verify → scope proposal → owner OK → build → PR → owner merges.

### Partial work worth preserving
- None uncommitted. Everything is in a PR (#86, #87) or merged. Scratchpad has commit-message drafts only (ephemeral). No stray branches beyond the pushed feature branches.
- LIVE STATE (verified 2026-07-27): 11 contracted funders; commission_records + (once #86 applies) bonus_records lifecycle wired; 0 real money rows (no genuine funding yet).
- Standing rule reminder for the applier: OLD CC 1 does NOT touch funder billing columns, CIPC data, or frontend.
- Last update: 2026-07-27 (Mon) — HANDOVER; OLD CC 1 stood down for rate-limit pause. Fresh session applies #86 post-merge.

## NEW CC 1 — Funder Billing + email footer + docs (STANDING DOWN — rate-limit pause)
- Last update: 2026-07-28 (Tue) — handover before strategic pause.

### ✅ Edge Function redeploy (DONE + verified)
- `send-notification-email` redeployed **v11 → v12** from merged `main` (after PRs #84 + #85 merged).
- Verified: footer address now **73 Marshall Street** (HTML + plain-text), no `75` anywhere; `verify_jwt=false` preserved (webhook-secret auth). Also brought the function current with `main` — the previously-undeployed C1.1 invoice email routing (FUNDER_INVOICE_ISSUED / INVOICE_MARKED_PAID / INVOICE_OVERDUE) is now live. This closes OLD CC 1's deferred "75→73 email-template TODO".

### ✅ SPEC S1 docs PR — ALREADY OPENED as **PR #88** (awaiting Macroscope + owner merge)
- NOT "planned/unopened" — it is open. A fresh session only needs to MERGE it, not create it.
- Content: SPEC.md S1 funder count `21 → 23` (+ derived appetite line 12 scored / 9→11 unscored, against 23; no invented scores); CLAUDE.md adds `Naledi` (Spartan) + `Sizwe` (AAA Consortium) to the fictional name pool.

### Funder billing block progress (funders INVOICE TO fields)
- **Pollen Finance** — full (pre-existing reference).
- **Merchant Capital** — DONE except **VAT** (owner to confirm number, or "not registered" → NULL). legal_name/address/CIPC/accounts_email/phone all set.
- **Bright On Capital** — name+legal_name set (renamed from "Brighton Capital"), short_code `BRIGHTON`. Address/CIPC/VAT/accounts_email still pending.
- **7 more contracted funders PENDING owner registered data** (address/CIPC/VAT/accounts_email): Bridgement, Sourcefin, GenFin, Better Banc, Flow48, Business Partners, + the 2 new (Spartan, AAA Consortium). ⚠️ Better Banc + Business Partners also lack a short_code.
- These details come from funder invoices/letterheads, not rate emails. Reconcile migration for all applied funder data = merged PR #85.
- Also captured (NOT applied): funder RATE-structure intel for C2 in scratchpad/funder-rate-structures-capture.md (Bright On 7.5% fees+interest, Sourcefin net-profit, Business Partners 1%, Spartan 1%, AAA 3%/opt1.6%, Centrafin 1%+2.8% client fee [NOT contracted]). Needs new rate_types → a C2 scope proposal.

- STANDING DOWN. No new work this session.

## DESIGN — UI polish + brand consistency (F5/F6 lane)
- Session: 2026-07-29 (Wed) ~21:25 SAST.
- Scope: empty states + loading states polish only. No partner/* routes, no C3–C7, no backend, no SPEC/CLAUDE/ROADMAP.
- **PR #89 — ✅ MERGED.** Branded empty state for `/leads` + new reusable `EmptyState` component (`src/components/ui/empty-state.tsx`). Filtered-empty vs first-run split.
- **PR #90 — OPEN (awaiting Macroscope + owner merge).** Branded empty state for `/clients` — reuses `EmptyState`, search-empty ("Clear search") vs first-run ("Add your first client"), lifted out of the table cell. Loading/error rows untouched. Visual-only, 1 code file, tsc+build+oxlint green.
- Note for other lanes: `EmptyState` (`src/components/ui/empty-state.tsx`) is the canonical empty-state primitive — reuse it, don't hand-roll "No X yet" text.
- Discrepancy flagged to owner: the briefing's `docs/proposals/f5-f6-ui-polish-scope-proposal.md` (Codex banked proposal) and `src/assets/fintech-spinner/` assets do NOT exist in this branch or origin/main. DESIGN lane is proceeding on the real live-code baseline instead.
- Queue: `/invoices` empty state APPROVED next (global CTA → `/pipeline?filter=funded`, verify param support first — else guidance-text-only). `/pipeline` empty column ON HOLD until BACK lands C3.
- Last update: 2026-07-29 (Wed).

## NEW CC 2 — Data Hygiene
- Session: not yet started
- Task: CIPC correction drafts + Brighton/Flow48 audit + sequence check + test residue scan
- Blocked on: owner CIPC portal lookup + Brighton/Flow48 review decision
- Last update: N/A
