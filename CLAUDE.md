# Fund Now Capital CRM — CLAUDE.md (v2)

## What this is
Internal CRM for **Fund Now Capital**, a South African SME funding brokerage owned by Thapelo Maupa (she/her). Owner-first tool now; referral-partner portal for **Doctor (Bright Destiny)** launches in **Phase D**. Client-facing portal deliberately deferred (Part 7).
Build order lives in **ROADMAP.md** — always consult it before starting work. Feature details live in `SPEC.md`.

## Who I'm working with
Thapelo — founder, 12+ years high-performance sales, learning to code (beginner), mother, Christian faith central to her life. Explain technical decisions in plain language. Name real tradeoffs once, recommend one, respect her call. Never invent funder rates, client data, or facts — ask. When she's working very late, be honest and encourage rest.

## Tech stack (locked)
React + TypeScript + Vite · Tailwind + shadcn/ui · Supabase (Postgres/Auth/Storage/RLS/Edge Functions) · react-hook-form + zod · TanStack Query · dnd-kit · gsap (PriorityGlow only) · Vercel (auto-deploy from main) · GitHub `maupatrades-cmd/Fund-Now-Capital-2026` · CodeRabbit on PRs · Resend for email · Twilio for WhatsApp/SMS (Phase D) · Anthropic API for AI features (Phase E).

## Security rules — NON-NEGOTIABLE
1. Supabase native Auth only; `auth.uid()`-based RLS on every table. NEVER custom session tokens (the LB-281 lesson from a previous build).
2. Service role key never reaches the client.
3. RLS on every new table from day one. Owner: full. Partner: read-only own rows. Funder identity is owner-only; partners see `display_name_for_partner` ONLY.
4. Public signup OFF. Users created by owner. Current users: `business.lekgoro@gmail.com` (owner), `queenasdice@gmail.com` (test partner — placeholder for Doctor).
5. Money is recomputed server-side on save; client values never trusted.
6. All monetary values `numeric(14,2)`. Never floats. Currency format `R1,250,000` (en-ZA).
7. Bank statements are owner-only in RLS — partners never see them.
8. Every new feature writes to `activity_logs` (once A10 lands) and, where user-relevant, fires notifications (once A11 lands).

## The Commission Engine (locked — implemented as `calculate_commission()` in Postgres)
Gross X → 40% company retention → 60% partner pool. Partner tier % of pool: PO deals flat 40%. Non-PO by gross band: R0–80,000 → 29% · R80,001–150,000 → 30% · R150,001–500,000 → 33% · R500,001+ → 25%. Owner share = pool − partner share. Three outputs always sum to X. Contract range 20–45%; all tiers comply. Single source of truth: the DB function, called via RPC everywhere (calculator screen, deal submissions, doctor earnings).

## Funder anonymisation (live in schema)
`funders.name` (owner-only) + `display_name_for_partner` (Rachel, Marcus, Ethan, Nadia, Palesa, Themba, Chloe, Amara, Ryan, Sophie, Sipho, Elizabeth, Thomas, Grace, Nicholas, Isabelle, Benjamin, Lerato, William, Zanele, Alexander, Naledi, Sizwe). Partner-facing surfaces show the fictional name only — screens, notifications, PDFs, AI outputs, everywhere.

## Brand
Navy `#1a3a52` · Teal `#2da8b8` · Green `#5dba5d` · Tagline "Many funders. More approvals." · Inter · clean modern SaaS · shared AppLayout (navy sidebar + top bar) on every owner screen · PriorityGlow is the single sanctioned glow effect (priority deals + celebrations only).

## Deal stages (15, Declined is terminal — revival only via deal-detail dropdown w/ confirm)
New Lead → Qualifying → Document Collection → Deal Review → Submitted → In Credit → Approved/Quote Received → Client Deciding → Verification/KYC → Contract Signed → Advance Pending → Funded → Invoiced → Commission Paid → Declined

## Current build state (update this section as phases complete)
DONE (applied + verified live): schema+RLS+engine+23 funders · auth/login · dashboard · funder panel · client DB · pipeline kanban + deal detail + calculators · A10 activity logging · A11 in-app notifications (bell/list/prefs, Realtime, owner-targeted role-aware triggers, mark-read RPCs return affected rows — PRs #18, #19) · A12 Resend email notifications (send-notification-email Edge Function via pg_net, branded HTML/text template, per-event email prefs, notification_deliveries email rows — PR #20; migration applied, function deployed, live DEAL_APPROVED smoke test passed end-to-end).
PHASE A FUNCTIONALLY COMPLETE: every code item (A1–A12) is done, applied, and verified live. A13 (Twilio/Meta WhatsApp Business API verification) is external paperwork (owner-driven) — now STARTED and running in parallel; not a code build.
EMAIL DESIGN SYSTEM: the four bulletproof email templates (welcome / deal_approved / weekly_summary / commission_paid) are the canonical foundation for every future email — documented in SPEC S16. Extend one of the four; never design a new email from scratch.
PHASE B IN PROGRESS: B1 (Industry Classification) done, live. B2.1 (Lead Entry) / B2.2 (Qualification Workflow — `qualify_lead`) / B2.3 (Duplicate Detection + lead activity logging + lead-lifecycle notifications & email) done, applied + verified live; B2.3 smoke test passed and test-data cleanup complete. **B3.1 (Document Management upgrade — foundation) DONE, applied + verified live.** Shipped in three PRs: schema (PR #42 + hotfix #43 — full 52-type taxonomy enum, audit-truth provenance + immutability, type-aware version supersede with a deferrable self-FK, period-scoped "pack" concurrency, expiry defaults, partner-aware RLS incl. the structured-PII exclusion list, storage-bucket RLS mirror) and owner-facing UI (PR #44 — `src/lib/documents.ts` taxonomy mirror, upgraded client Documents tab with pack view + version history + expiry pills + uploaded-by badges, owner-only `/documents` global page, deal-detail read-only client-documents view). 9-point smoke test passed live; **first real documents in the CRM** (Mama Mabase JV: CIPC + 2 bank-statement months with versioning). **B3 expiry-alert automation DONE, applied + verified live** (PR #46 — `check_document_expiries()` pg_cron sweep daily 04:00 UTC / 06:00 SAST, once-per-(document,threshold) dedup ledger, pack-aware bodies, auto-mark expired, `weekly_summary` email per S16.4; first live cron run 2026-07-19 04:00 UTC succeeded, 0 alerts as expected). **B3.2 (per-document verification workflow) DONE, applied + verified live** (PR #47 — `verification_status`/`rejection_reason`/`verified_by`/`verified_at` + `documents_lead_deal_orthogonal` CHECK; owner-only `set_document_verification` RPC with its own activity-log write; `lead_required_docs_missing` qualification gate wired into `qualify_lead(lead_id, override)` with a typed-`OVERRIDE` escape hatch that logs the missing categories; lead→client document re-pointer at qualify; partner-safe `LEAD_DOCUMENT_REJECTED` notification via `weekly_summary`; entity-parameterised Documents surface (`DocEntity = {kind, id}`) now on both client + lead detail; `send-notification-email` redeployed v10 + readiness-probed green; migrations applied in order, DO-block assertions passed). **PHASE B M1 (Document Management upgrade — B3) IS NOW COMPLETE.** C0 money-ops foundation + S7A commission lifecycle specced ahead of Phase C (PR #48, docs only). **B4 (Client Story + Call Logs) DONE, applied + verified live** (PR #50 — `client_stories` 1:1 narrative record (UNIQUE client_id, create-on-first-save upsert), append-only `client_story_notes` "ongoing impressions" (SELECT+INSERT-only RLS, never edited/deleted), `call_logs` with `call_medium` enum + `call_logs_followup_needs_date` CHECK, `call_log_followup_alerts` claim-then-act dedup ledger; owner-only RLS on all four; `log_activity()` extended with `client_story` + `call_log` branches; `followups_due()` detection + `check_followups_due()` pg_cron sweep daily 04:15 UTC, FOLLOW_UP_DUE owner-pref backfill + `weekly_summary` email per S16.4; owner-facing Story + Call Log tabs on client detail (`StoryPanel`, `CallLogPanel`, `useClientStory`/`useCallLogs`); migrations applied in order + DO-block assertions passed, `send-notification-email` redeployed v11, live sweep smoke test confirmed end-to-end (in-app delivered + email sent + dedup verified) then test data removed). **PHASE B M2 (Client Story + Call Logs — B4) IS NOW COMPLETE.**
**PHASE B COMPLETE (verified against live DB 2026-07-31, full-system audit — see `AUDIT.md`):**
- **B4.1 (Client Story on lead detail) DONE, applied + verified live** (PR #63 window — `client_stories` re-keyed to nullable `client_id` XOR `lead_id`, story + notes re-pointed to the client at qualify (`STORY_MIGRATED_LEAD_TO_CLIENT` activity event), `StoryPanel` entity-parameterised onto lead detail, markdown export).
- **B5.2 (client-side duplicate detection) DONE, applied + verified live** (PRs #61/#62 — `find_client_duplicates` RPC, server-side `clients_cipc_block` BEFORE-trigger, clients CIPC unique index, client-form dup UX). **B5 (SA Data Validation) IS COMPLETE** (B5.1 was PR #53).
- **B6 (Stakeholders) DONE, applied + verified live** (PRs #57 docs / #58 B6.1 schema / #59 B6.2 UI / #60 B6.3 qualify re-pointer — `client_stakeholders` one-row-per-person with `roles[]`, SA-ID/passport branch + ISO country, shareholding + FICA beneficial-owner auto-suggest, XOR lead/client CHECK, owner-full RLS + PII-excluded `partner_stakeholders_view`, `log_activity` branch, stakeholder lead→client re-pointer in `qualify_lead`; Directors & Shareholders UI on lead + client detail). 0 rows live — owner enters real cap-table data (no backfill, per S3a decision 5).
PHASE C IN PROGRESS (backend through C2 fully live; audited 2026-07-31):
- **C0 (Money-ops foundations) DONE, applied + verified live** (PRs #65 C0.1 schema / #66 C0.2 `/settings/funders` UI / #68 C0.3 contracted-gate + `percent_of_finance_charge` + `payment_terms_days` / #70 short_code unique index). `funder_commission_structures` + `funders.is_contracted`/`short_code` + effective-dated `funder_rate_at()` lookup. Live: 11 contracted funders; 4 rate rows (Business Partners 1% · Flow48 2.5% · Merchant Capital 3.5% gross-funded · Pollen 10% of finance charge) after the FIX #2 non-contracted cleanup (PR #93, applied). ⚠️ Better Banc + Business Partners still missing an explicit `short_code` (owner data entry; invoicing does NOT block — `funder_shortcode()` falls back to a name-derived code, but a deliberate code should be set before first invoicing them — AUDIT.md F6).
- **C1 (FNC → funder invoicing) DONE, applied + deployed** (C1.1 backend PR #72 + logo hotfixes #74–#76 — `funder_invoices` single-table design, `generate/issue/mark-paid/void` RPCs with advisory locks + state guards, `generate-invoice-pdf` Edge Function (v6) + private `invoices` bucket, `check_overdue_invoices()` pg_cron 04:30 UTC; C1.2 UI PR #78 — deal-detail Generate button, `/invoices` + `/invoices/:id`, owner-session PDF download, `update_draft_invoice`). Sequence starts INV-0032; **0 invoices issued yet** (no genuine funding).
- **C2 (commission records wiring) DONE, applied + verified live** (C2.1 `deals.deal_type` cutover PR #79 · C2.2 `commission_records` reshape + `write_commission_record` + the two funded triggers PRs #80/#82 · C2.3 funder-invoice-driven state cascade (earned→outstanding→payable) PR #83 · C2.4 `bonus_records` + BONUS_PAID PR #86 — **applied to live**, both migrations in order, verified). `commission_state` = earned/outstanding/payable/settled/void; settle/unsettle are P0002 stubs until C4. `commission_records` + `bonus_records` **legitimately 0 rows** (see the "No deal is GENUINELY funded yet" note).
- **C3 (Doctor's earnings) PARTIAL — 🔨:** owner-side `/partner-earnings` reconciliation view shipped (PR #91). The S8 `doctor_earnings` lifecycle tables + partner-facing surface are NOT built (Phase D consumes them; S7C presentation policy governs).
AUDIT (2026-07-31): full read-only system audit in **`AUDIT.md`** (repo root). No blockers. FIX lane: FIX #1 docs (PR #92, merged) · FIX #2 rate cleanup (PR #93, merged + applied) · notification label/icon fix **PR #94 OPEN** · grants-hardening migration (audit FIX-A) queued.
REAL LIVE DATA (only production rows in the DB — all test leads/deals/clients removed 2026-07-17): Mama Mabase JV (DEAL-001, at **Funded stage** but NOT genuinely funded — no `amount_funded` captured; see the "No deal is GENUINELY funded yet" prep note below; first real documents attached — CIPC + bank statements) · Fepa Sechaba Milling and Mining (DEAL-002) · NRL BAKWENA MINE (DEAL-004).
NEXT: audit FIX-A (RPC grants hardening migration) + FIX-B (notification prefs matrix — after PR #94 lands) · first real Client Story + Call Log capture (Fepa Sechaba / Tumi) · C3 completion (S8 `doctor_earnings` lifecycle — scope proposal first) · owner rulings before first affected deal: S7C PO-tier scope (Sourcefin-only vs all-PO) + R1M+ tier · A13 paperwork (external, in progress).

## Open decisions (do not build past these without owner's answer)
1. ~~Doctor's Commission Estimator "Business View" transparency (blocks D5).~~ **RESOLVED (owner, 2026-07-21):** his-number-only, presented as a display-only 50/50 split — see SPEC S11.1 + S7C/S7C.1. D5 unblocked.
2. Password expiry policy at F2 (recommend against forced 90-day rotation; prefer 2FA).
3. Commission-engine SPEC reconciliation (owner ruling before first affected deal — separate scope proposal, do NOT change `calculate_commission` without sign-off): (a) flat-40% PO tier applies to ALL PO deals in code vs Sourcefin-only in SPEC S7C; (b) R1M+ band is 25% in code vs TBD in S7C.

## Preparation notes for future phases
External, owner-driven pre-work that must be lined up BEFORE a build phase opens (same category as A13's Twilio/Meta WhatsApp certification — paperwork, not code).
- ⚠️ **Phase E4 legal review pre-work required.** Two documents need external legal-advisor review BEFORE the E4 build opens (roughly 4–6 weeks from July 2026): (1) **Broker Referral Consent template** — NCR/FAIS intermediary-distinction language; (2) **Client + Partner T&Cs** — NCR/FAIS/POPIA compliance language. Owner should engage the legal advisor by ~week 3 of Phase C to allow 2–3 weeks of legal turnaround before E4 starts. Otherwise E4 code ships ready but stalls on template content. (Scope: SPEC S17 Consent Pack + T&Cs.)
- 🔒 **Commission presentation policy locked (2026-07-20) — gates every Doctor-facing money surface (C2/C3/C4, D5).** Backend `commission_records` stores the REAL tiered math (FNC gross → 40% company retention / 60% partner pool → Doctor's tier% of the pool → owner remainder). Doctor/partner surfaces — the Phase D portal, C4 partner-invoice PDFs, the D5 estimator — show **only Doctor's commission in Rands**, framed as a **50/50 partner split**; they **NEVER** reveal FNC gross, company retention, the partner-pool amount, the tier band, or the tier %, nor the funder's rate structure. Full spec + the locked tier table in **SPEC S7C**. Respect this before building any partner-facing commission view.
- 💰 **No deal is GENUINELY funded yet — commission_records is legitimately empty (0 rows).** DEAL-001 (Mama Mabase JV) is at kanban stage **`Funded` but is NOT genuinely funded** — it was advanced to Funded (2026-07-22) with **no `amount_funded`/`amount_approved`** captured on its submissions (still quote_received/approved). Earlier session notes that treated it as truly Funded were premature. C2's commission auto-write (`write_commission_record` + the funded triggers) only fires when a deal reaches Funded **AND** a submission carries `amount_funded`; since no real deal has that, `commission_records` (and every C2.3/C2.4 downstream: outstanding/payable/settled, bonuses) is correctly empty. **The owner progresses deals through funder submission → approved → funded as real business events happen, capturing `amount_funded` at funding — never backfilling from notes.** So the first real commission (and any bonus) will appear only when a genuine funding event is entered in the UI.

## Working style
Small verified stages. One PR per logical change; open PR, WAIT for CodeRabbit, owner merges. After any schema merge: apply migration to live DB, verify, then owner runs smoke test. Tell her what you did and what to test — never chain ten silent changes. Ask before destructive migrations. Never discuss real funder rates in partner-facing code or copy.

Docstring coverage is intentionally deprioritized in favor of inline comments explaining WHY and TypeScript types documenting SHAPE. CodeRabbit's default 80% docstring threshold is treated as informational only.

For any migration that adds FK columns to a table with existing data, use `NOT VALID` for the FK constraints and build their indexes with `CREATE INDEX CONCURRENTLY` in a follow-up migration (outside any transaction — `CONCURRENTLY` cannot run inside one), then `VALIDATE CONSTRAINT`. Never use inline `REFERENCES` or plain `CREATE INDEX` on populated tables — these acquire write-blocking locks. Applies to every FK migration in Phases B through F.

PostgREST `!inner` joins require the related row to exist. Any join on a nullable FK column MUST use `!left`, not `!inner`, or legacy rows will vanish from queries. Applies especially to `industry_id`, `sub_industry_id`, and any future nullable FK to leads/deals/clients relationships.

For Edge Functions invoked from the database via `pg_net` (currently `send-notification-email`, and any future notification/webhook-invoked function), a `WEBHOOK_SECRET` must be set as a Supabase Edge Function secret AND stored as a Vault value in the database so both sides agree. Every Edge Function may also require its own function-specific environment variables (API keys, base URLs). When a Claude Code session's proxy blocks outbound HTTP, use `pg_net` from inside the database for readiness probes — it's the same path production uses and stays non-destructive with fake IDs.

Supabase RLS silently returns empty result sets for UPDATE/DELETE without permission. Every mutation must use `.select()` or `RETURNING id` and check the returned row count to catch silent RLS failures loudly. Never assume a mutation succeeded just because no error was thrown.

CodeRabbit response workflow: when CodeRabbit posts a review, read findings carefully, propose fixes with brief rationale (accept / skip / adjust), wait for owner approval, then apply the approved fixes and wait for re-review. Do NOT auto-apply CodeRabbit findings. Even trivial nitpicks go through the propose-approve-fix-review loop. The owner stays in the decision seat on every change.
